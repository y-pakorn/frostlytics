NEXT_PUBLIC_APP_URL=http://localhost:3000

# Elysia API + backfill (`pnpm server`, `pnpm backfill`). Use a standard libpq URL; add `?sslmode=require` if your host needs TLS.
# DATABASE_URL=postgresql://user:password@localhost:5432/walrus